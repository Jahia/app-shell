JAHIA_VERSION=${JAHIA_VERSION:-LATEST}
JAHIA_IMAGE=${JAHIA_IMAGE:-jahia/jahia-ee-dev:8-SNAPSHOT}
TESTS_IMAGE=${TESTS_IMAGE:-jahia/app-shell:latest}
MODULE_ID=${MODULE_ID:-app-shell}
MANIFEST=${MANIFEST:-provisioning-manifest-snapshot.yml}
JAHIA_URL=${JAHIA_URL:-http://localhost:8080}
SUPER_USER_PASSWORD=${SUPER_USER_PASSWORD:-root1234}
NEXUS_USERNAME=${NEXUS_USERNAME:-NEXUS_USERNAME}
NEXUS_PASSWORD=${NEXUS_PASSWORD:-NEXUS_PASSWORD}

CLOUD_JSESSIONID=${CLOUD_JSESSIONID:-empty}
CLOUD_ENVNAME=${CLOUD_ENVNAME:-qatest}
CLOUD_DOMAINSUFFIX=${CLOUD_DOMAINSUFFIX:-jahiadevs}
CLOUD_JAHIA_VERSION=${CLOUD_JAHIA_VERSION:-8.1.6.0}
CLOUD_SUBSCRIPTIONITEMID=${CLOUD_SUBSCRIPTIONITEMID:-193}
CLOUD_REGIONID=${CLOUD_REGIONID:-ovh-fr-1}
CLOUD_BANDID=${CLOUD_BANDID:-ovh-fr-1}