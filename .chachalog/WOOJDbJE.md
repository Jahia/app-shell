---
# Allowed version bumps: patch, minor, major
app-shell: major
---

Opened the migration window for Vite-powered UI extensions. (#373)

The app-shell now provides all the singleton libraries (e.g. React).
